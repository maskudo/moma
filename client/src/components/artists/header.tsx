'use client';

import { cn } from '@/lib/utils';
import * as React from 'react';

const Header: React.FC = () => {
  return (
    <div className={cn(['flex items-center justify-between text-b1'])}></div>
  );
};

export default Header;

